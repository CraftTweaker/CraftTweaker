apply plugin: 'base' // To add "clean" task to the root project.
apply from: 'configuration.gradle'
apply plugin: "maven"
subprojects {
    apply from: rootProject.file('common.gradle')
}
repositories {    
    mavenLocal()
    mavenCentral()
    maven {
        name "Tesla"
        url 'http://maven.epoxide.xyz'
    }
    maven {
        name 'Mobius'
        url 'http://mobiusstrip.eu/maven'
    }
    maven {
        name 'JEI'
        url "http://dvs1.progwml6.com/files/maven"
    }
}
configurations {
    deployerJars
}
dependencies {
    deployerJars 'org.apache.maven.wagon:wagon-ftp:2.2'
}

task("uploadJars", dependsOn: "reobf") {
    description = "uploads JARs"
    if (System.getenv("bamboo_local_maven") != null) {
        uploadArchives {
            repositories {
                mavenDeployer {
                    repository(url: "file://" + System.getenv("bamboo_local_maven"))
                    pom {
                        groupId = project.group
                        version
                        artifactId = project.archivesBaseName
                        project {
                            name project.archivesBaseName
                            packaging 'jar'
                            description 'CraftTweaker'
                            url 'http://BrassGoggledCoders.xyz'
                            scm {
                                url 'https://github.com/jaredlll08/CraftTweaker.git'
                                connection 'scm:git:git@github.com:jaredlll08/CraftTweaker.git'
                                developerConnection 'scm:git:git@github.com:jaredlll08/CraftTweaker.git'
                            }
                            issueManagement {
                                system 'github'
                                url 'https://github.com/jaredlll08/CraftTweaker/issues'
                            }
                            developers {
                                developer {
                                    id 'Jared'
                                    name 'Jared'
                                    roles { role 'developer' }
                                }
                            }
                        }
                    }
                }
            }
        }
    } else {
        println "Local_Maven not set"
    }
}

task wrapper(type: Wrapper, description: 'Creates and deploys the Gradle wrapper to the current directory.') {
    gradleVersion = '2.6'
}

subprojects.each { subproject -> evaluationDependsOn( subproject.path ) }

task setupDecompWorkspaceAll(dependsOn: projects1710.collect { ':' + it + ":setupDecompWorkspace" } + projects18.collect { ':' + it + ':setupDecompWorkspace'} + projects1102.collect { ':' + it + ':setupDecompWorkspace'}) {}

task setupCIWorkspaceAll(dependsOn: projects1710.collect { ':' + it + ":setupCIWorkspace" } + projects18.collect { ':' + it + ':setupCIWorkspace'} + projects1102.collect { ':' + it + ':setupCIWorkspace'}) {}

task assembleMC1710(type: Jar, dependsOn: [\
		':ZenScript:build',
		':MineTweaker3-API:build']
		+ projects1710.collect { ':' + it + ":build" }) {
	archiveName="CraftTweaker-1.7.10-${mineTweakerVersion}.jar"
	
	from zipTree(project(":ZenScript").jar.archivePath)
	from zipTree(project(':MineTweaker3-API').jar.archivePath)
	
	for (p in projects1710) {
		from zipTree(project(p).jar.archivePath)
	}
}

task assembleMC18(type: Jar, dependsOn: [\
		':ZenScript:build',
		':MineTweaker3-API:build']
		+ projects18.collect { ':' + it + ":build" }) {
	archiveName="CraftTweaker-1.8.9-${mineTweakerVersion}.jar"
	
	from zipTree(project(":ZenScript").jar.archivePath)
	from zipTree(project(':MineTweaker3-API').jar.archivePath)
	
	for (p in projects18) {
		from zipTree(project(p).jar.archivePath)
	}
}

task assembleMC1102(type: Jar, dependsOn: [\
		':ZenScript:build',
		':MineTweaker3-API:build']
		+ projects1102.collect { ':' + it + ":build" }) {
	archiveName="CraftTweaker-1.10.2-${mineTweakerVersion}.jar"
	
	from zipTree(project(":ZenScript").jar.archivePath)
	from zipTree(project(':MineTweaker3-API').jar.archivePath)
	
	for (p in projects1102) {
		from zipTree(project(p).jar.archivePath)
	}
}

task assembleDevBase1710(type: Jar, dependsOn: [
		':ZenScript:build',
		':MineTweaker3-API:build',
		':MineTweaker3-MC1710-Main:build']) {
	archiveName="CraftTweaker-Dev-1.7.10-${mineTweakerVersion}.jar"
	
	from project(':ZenScript').sourceSets.main.output
	from project(':MineTweaker3-API').sourceSets.main.output
	from project(':MineTweaker3-MC1710-Main').sourceSets.main.output
}

task assembleDevBase18(type: Jar, dependsOn: [
		':ZenScript:build',
		':MineTweaker3-API:build',
		':MineTweaker3-MC18-Main:build']) {
	archiveName="CraftTweaker-Dev-1.8.9-${mineTweakerVersion}.jar"
	
	from project(':ZenScript').sourceSets.main.output
	from project(':MineTweaker3-API').sourceSets.main.output
	from project(':MineTweaker3-MC18-Main').sourceSets.main.output
}

task assembleDevBase1102(type: Jar, dependsOn: [
		':ZenScript:build',
		':MineTweaker3-API:build',
		':MineTweaker3-MC1102-Main:build']) {
	archiveName="CraftTweaker-Dev-1.10.2-${mineTweakerVersion}.jar"
	
	from project(':ZenScript').sourceSets.main.output
	from project(':MineTweaker3-API').sourceSets.main.output
	from project(':MineTweaker3-MC1102-Main').sourceSets.main.output
}

task assembleDevFull1710(type: Jar, dependsOn: [
		':ZenScript:build',
		':MineTweaker3-API:build']
		+ projects1710.collect { ':' + it + ":build" }) {
	archiveName="CraftTweaker-Dev-Full-1.7.10-${mineTweakerVersion}.jar"
	
	from project(':ZenScript').sourceSets.main.output
	from project(':MineTweaker3-API').sourceSets.main.output
	
	for (p in projects1710) {
		from project(p).sourceSets.main.output
	}
}


task assembleDevFull18(type: Jar, dependsOn: [
		':ZenScript:build',
		':MineTweaker3-API:build']
		+ projects18.collect { ':' + it + ":build" }) {
	archiveName="CraftTweaker-Dev-Full-1.8.9-${mineTweakerVersion}.jar"
	
	from project(':ZenScript').sourceSets.main.output
	from project(':MineTweaker3-API').sourceSets.main.output
	
	for (p in projects18) {
		from project(p).sourceSets.main.output
	}
}

task assembleDevFull1102(type: Jar, dependsOn: [
		':ZenScript:build',
		':MineTweaker3-API:build']
		+ projects1102.collect { ':' + it + ":build" }) {
	archiveName="CraftTweaker-Dev-Full-1.10.2-${mineTweakerVersion}.jar"
	
	from project(':ZenScript').sourceSets.main.output
	from project(':MineTweaker3-API').sourceSets.main.output
	
	for (p in projects1102) {
		from project(p).sourceSets.main.output
	}
}

task assembleAll(dependsOn: [
	assembleMC1710,
	assembleMC18,
	assembleMC1102,
	assembleDevBase1710,
	assembleDevBase18,
	assembleDevBase1102,
	assembleDevFull1710,
	assembleDevFull18,
	assembleDevFull1102]) {
}

configurations {
    deployerJars
}





